spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/dchalops/microservices
          search-paths: config
          username: dchalops
          password: Chalo2130198720
